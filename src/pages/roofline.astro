---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Roofline Analysis - Vibe AI Infra">
  <div class="page-container">
    <div class="hero">
      <h1>Roofline Analysis</h1>
      <p class="subtitle">
        AI-generated roofline performance analysis for various hardware architectures.
        Coming soon...
      </p>
    </div>

    <div class="placeholder">
      <div class="placeholder-icon">📊</div>
      <h2>Performance Visualization Tools</h2>
      <p>
        This section will host interactive roofline model visualizations and analysis tools
        for evaluating compute vs. memory-bound operations across different hardware platforms.
      </p>
    </div>
  </div>
</Layout>

<style>
  .page-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 60px 20px;
  }

  .hero {
    text-align: center;
    margin-bottom: 60px;
  }

  .hero h1 {
    font-size: clamp(2.5rem, 6vw, 3.5rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin-bottom: 16px;
    background: linear-gradient(135deg, #0a0a0a 0%, #6366f1 50%, #ec4899 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    font-size: 1.125rem;
    color: var(--muted);
    line-height: 1.6;
  }

  .placeholder {
    text-align: center;
    padding: 80px 40px;
    background: white;
    border: 1px solid var(--border);
    border-radius: 16px;
  }

  .placeholder-icon {
    font-size: 4rem;
    margin-bottom: 24px;
  }

  .placeholder h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 16px;
  }

  .placeholder p {
    font-size: 1.125rem;
    color: var(--muted);
    line-height: 1.7;
    max-width: 600px;
    margin: 0 auto;
  }
</style>
